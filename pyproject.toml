[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
source = ["agents", "api", "models", "experiments"]
omit = ["tests/*"]

[tool.coverage.report]
fail_under = 65
show_missing = true
